%p= link_to 'List of available currencies', currencies_path
- if user_signed_in?
  %p= link_to 'Create a trip', new_trip_path
  %p= link_to 'Trips list', trips_path

%input#country(type="text" readonly)
%input#country_code(type="hidden")
%p
  Country name:
  %span#country-name
%p
  Country code:
  %span#country-code
%p
  Country currency:
  %span#country-currency
%p
  Country currency code:
  %span#country-currency-code
